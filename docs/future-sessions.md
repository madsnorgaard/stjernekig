# Future sessions — parking lot

Topics colleagues have mentioned wanting, in no particular order.

- **Session 03 — Sub-agents and parallel research.** The `Agent` tool with the `Explore` subagent. Example: "find every external-HTTP call in the project and tell me which ones lack a timeout."
- **Session 04 — Hooks and `/update-config`.** Pre/post hooks in `settings.json`. The `update-config` skill. The `fewer-permission-prompts` skill for cutting noise.
- **Session 05 — `/loop` and `/schedule`.** Long-running tasks. The daily APOD cron is a real candidate for a scheduled routine.
- **Session 06 — Tests.** PHPUnit + Drupal test kit, asking Claude to write tests for the worker.
- **Session 07 — Migrating an existing site.** A real client repo, Drupal 7 → 10 migration plan in plan-mode.
- **Session 08 — Code review with `/ultrareview`.** Hand a real PR to multi-agent cloud review.

Pick the next two topics based on what colleagues actually ask for after session 01 and 02.
